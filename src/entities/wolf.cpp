#include "wolf.h"

Wolf::Wolf(hlam::Vec2 pos, hlam::Vec2 size, hlam::Vec2 speed, hlam::Vec2 runSpeed)
    : pos(pos), size(size), speed(speed), runSpeed(runSpeed) {}

void Wolf::Update(float dt) {}

void Wolf::Draw() {}
